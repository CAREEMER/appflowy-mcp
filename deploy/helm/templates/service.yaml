apiVersion: v1
kind: Service
metadata:
  name: {{ include "appflowy-mcp.fullname" . }}
  labels:
    {{- include "appflowy-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "appflowy-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
